# Project Commands

Always use `pnpm` for all commands. Never use `npm`, `yarn`, or `npx`.

- **Dev server:** `pnpm dev`
- **Build:** `pnpm build`
- **Test:** `pnpm test`
- **Playwright tests:** `pnpm test:playwright`
- **Lint:** `pnpm lint`
- **Fetch test fixtures:** `pnpm fetch-fixture`
